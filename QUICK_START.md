# 🚀 Быстрый запуск локально

## Ответы на ваши вопросы

### ❓ Можно ли запустить локально?
**Да!** Можно запустить локально для тестирования.

### ❓ Будет ли использоваться Supabase и Railway?
**Гибридный вариант (рекомендуется):**
- ✅ **База данных**: Используется **Supabase** (удалённая) - все миграции уже там применены
- ✅ **Auth**: Используется **Supabase Auth** (удалённый)
- ✅ **Backend**: Запускается **локально** на `http://localhost:8000`
- ✅ **Frontend**: Запускается **локально** на `http://localhost:5173`

**Почему так?**
- Не нужно настраивать локальную PostgreSQL
- Все миграции уже применены на Supabase
- Быстрый старт без дополнительной настройки

---

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка (уже сделано автоматически)

✅ Виртуальное окружение Python создано  
✅ Зависимости backend установлены  
✅ Зависимости frontend установлены  
✅ `.env` файлы настроены для локальной разработки

### Шаг 2: Запуск Backend

Откройте **первый терминал**:

```powershell
cd backend
.venv\Scripts\activate
uvicorn app.main:app --reload
```

Вы должны увидеть:
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

**Проверка:** Откройте http://localhost:8000/docs - должна открыться документация API.

### Шаг 3: Запуск Frontend

Откройте **второй терминал** (новое окно PowerShell):

```powershell
cd frontend
npm run dev
```

Вы должны увидеть:
```
  VITE v7.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**Проверка:** Откройте http://localhost:5173 - должна открыться главная страница.

---

## 🔍 Что происходит под капотом?

### Frontend → Backend
```
http://localhost:5173 (Frontend)
    ↓ HTTP запросы
http://localhost:8000 (Backend)
    ↓ SQL запросы
Supabase PostgreSQL (удалённая БД)
```

### Аутентификация
```
Frontend → Supabase Auth (удалённый)
    ↓ JWT токен
Backend → Проверка токена через Supabase
```

---

## ⚙️ Настройки

### Backend (.env)
- `ALLOWED_ORIGINS=http://localhost:5173` - разрешает запросы с фронтенда
- `DATABASE_URL=postgresql://...` - подключение к Supabase
- `SUPABASE_URL` и ключи - для работы с Supabase Auth

### Frontend (.env.local)
- `VITE_BACKEND_URL=http://localhost:8000` - адрес локального backend
- `VITE_SUPABASE_URL` и ключи - для Supabase Auth

---

## 🧪 Тестирование

1. **Регистрация:** http://localhost:5173/register
2. **Вход:** http://localhost:5173/login
3. **Dashboard:** http://localhost:5173/dashboard
4. **API Docs:** http://localhost:8000/docs

---

## 🐛 Troubleshooting

### Backend не запускается

**Ошибка:** `ModuleNotFoundError`
```powershell
# Установите зависимости
cd backend
.venv\Scripts\activate
pip install -r requirements.txt
```

**Ошибка:** `Connection refused` к БД
- Проверьте `DATABASE_URL` в `backend/.env`
- Убедитесь, что Supabase доступен

### Frontend не подключается к Backend

**Ошибка:** CORS error
- Проверьте `ALLOWED_ORIGINS=http://localhost:5173` в `backend/.env`
- Перезапустите backend после изменения .env

**Ошибка:** `Network Error`
- Убедитесь, что backend запущен на порту 8000
- Проверьте `VITE_BACKEND_URL=http://localhost:8000` в `frontend/.env.local`

### Порт занят

**Backend (8000):**
```powershell
# Используйте другой порт
uvicorn app.main:app --port 8001
# И обновите frontend/.env.local:
# VITE_BACKEND_URL=http://localhost:8001
```

**Frontend (5173):**
```powershell
# Vite автоматически найдёт свободный порт
# Или укажите явно:
npm run dev -- --port 5174
```

---

## 📝 Полезные команды

### Backend
```powershell
# Запуск с автоперезагрузкой
uvicorn app.main:app --reload

# Запуск на другом порту
uvicorn app.main:app --port 8001

# Проверка синтаксиса
python -m compileall app
```

### Frontend
```powershell
# Dev сервер
npm run dev

# Сборка
npm run build

# Линтинг
npm run lint
```

---

## ✅ Готово!

Теперь у вас работает:
- ✅ Локальный frontend на http://localhost:5173
- ✅ Локальный backend на http://localhost:8000
- ✅ Подключение к Supabase (БД и Auth)

**Все изменения в коде будут автоматически перезагружаться!**


{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo '=== STARTING BUILD ===' && echo 'Building frontend...' && cd frontend && npm install && npm run build && echo 'Frontend built!' && cd .. && echo 'Installing backend...' && cd backend && python3 -m pip install -r requirements.txt && echo '=== BUILD COMPLETE ==='"
  },
  "deploy": {
    "startCommand": "bash start.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

